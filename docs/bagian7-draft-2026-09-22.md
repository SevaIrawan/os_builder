# Draf Bagian 7 — 七、政策参数落位

> Target node: `9302bb14-5f70-3817-8038-c9ef0f97c421` (paragraf `🔲 待填。` di bawah judul 七).
> Operasi: 1 `replaceNode` + 2 `insertNodeAfter` (urutan kirim = urutan tampil). Total 7.305 karakter.
> **Belum ditulis ke Confluence.**

---

## (1) Paragraf pembuka — `replaceNode`

🔲 待填。「**【2026-09-22 补·本区已填写】**本区按 04.5.2（现行 **v11**）模板五列登记：参数键／白话／住处／变更入口／生效方式。上句「🔲 待填」为本次之前的状态，**原文保留不删**；自本次起本区以下表为准，仍未落位的格子在表内**逐格**标 🔲，不以整区占位代替。模板对本区的定义逐字为：「『哪些数字会变、住哪、谁改、怎么生效』的参数化登记……政策的**业务含义**人读权威住政策制度页（Spec 引用区链接），本区只登记参数化落位；仍硬编码在代码内的项如实标注为数据化候选」——故本区**不复述政策数值的业务含义**，那住 Spec。两条前提先写在表外：**①本流程没有政策表。**2026-09-22 实读 04.9（现行 **v106**）§四，全站 12 张 Data Table 中唯一分类为「政策表」的是 `NOS Policy Store (F2 11-col)`（`DsK1slgPdy79LPHu`），用途逐字为「假期政策数值的运行时权威」，与本流程无关；同节并定「建表权仅平台 Owner；建造线需要新表向平台 Owner 申请」。本页第六区亦未登记 `Policy Engine` 为本流程调用件。故本流程工程侧的数值目前一律**没有数据行落点**，按模板要求如实标「数据化候选」——此为结构事实，非本侧遗漏，与员工离职建造单第七区先例一致。**②模板要求的「人读权威政策页」本流程暂缺。**Spec ⑤ 引用区指向《Nexmax WFH工作规章制度（正式版）》3.3节，该页本侧两个执行身份均读不到（见页首附表第 16 行），正式位置与版本待流程 Owner 提供；在其补齐前，本区各行的「业务含义」只能回指 Spec。」

---

## (2) Tabel — `insertNodeAfter`

| # | 参数键 | 白话 | 住处 | 变更入口 | 生效方式 |
|---|---|---|---|---|---|
| 1 | **PIP 延长周期＝15 天／30 天／60 天／90 天** | PIP 延长一次时可选的天数档位，只有这四档，不开放「视情况」自填 | Jira option 字段 `customfield_18253`（PIP Extension Period）的**选项集**，不是代码常量。本侧 2026-09-22 以 `createmeta` 逐值回读（HR × `Sub-ticket` 14316） | Schema Owner（Kent）按 04.10 §五 改选项；业务口径改动先经流程 Owner——Felix c50328 已定「就 4 档」，Kent c50343 据此锁定 | 改选项即时生效。消费方一律按字面值取，**不得在件内另写一份映射表**（会与选项集漂移） |
| 2 | **Show Cause 解释结果＝成立／不成立** | N10 判员工解释成不成立的两个取值，对应 D-5 文案【Explanation Accepted / Not Accepted】 | `customfield_18252` 选项集（2026-09-22 已回读） | Schema Owner（Kent）按 04.10 §五 | 改选项即时生效；D-5 文案占位符按字面值填充 |
| 3 | **PIP Result＝Completed／Extension／Failed** | N17 的 PIP 过程结果三值，独立于 Final Outcome | `customfield_18254` 选项集（2026-09-22 已回读） | Schema Owner（Kent）按 04.10 §五 | 改选项即时生效 |
| 4 | **判定依据类型＝纪律违规／PIP未改善** | N14／N17 判定后系统写入的依据分类，供离职流「辞退原因」分类对齐 | `customfield_18255` 选项集（2026-09-22 已回读） | Schema Owner（Kent）按 04.10 §五。**跨流程约束**：两值须与离职流「辞退原因」（Felix c50045）逐字一致 | 改选项即时生效；**单改本侧即破坏与离职流的对齐**，改前须同时核离职流该字段取值 |
| 5 | **联合评审结果＝通过／不通过** | N25 联合评审的结论两值：通过→N27 已解除；不通过→维持 N13 Active | `customfield_18257` 选项集（2026-09-22 已回读；`Task` 10004 与 `Sub-ticket` 14316 两处屏上均在位） | Schema Owner（Kent）按 04.10 §五 | 改选项即时生效 |
| 6 | **SLA C-1～C-20（17 条时限）** | 各节点多久算超时、超时提醒谁、升级给谁 | **人读权威住 Spec C 表**（现行 v62）。工程载体按 04.4 §8.1 分工：主单侧（含「待审批」与整单交付时限）走 JSM 原生 SLA 引擎；子单与跨对象时限走 n8n 定时扫描件。**两者均未建**——SSCSD-411 实测主单挂的仍是 Jira 默认 8h／16h，非本流程数值。→ **数据化候选** | 数值改动＝改 Spec C 表，属流程 Owner（Felix）；**建设侧不得改 Spec 语义**（04.5 §五 写权分离）。工程侧照 Spec 配置由建造侧执行 | 🔲 未建。建成后按页首附表第 21 行四项判据验收：①JSM SLA 按 C 表改配并 API 回读、清除默认 8h／16h；②子单与跨对象落 n8n 扫描件；③breach 经 webhook 出 n8n（不得用 Jira Automation 直发 Slack，04.4 §8.2）；④Calendar 与 NTP 工作时口径一致（04.4 §8.3） |
| 7 | **纪律记录有效期 参考值＝Verbal 3 个月／Written 6 个月／Final Written「永不自动失效」** | Warning 多久自动重置。**这是参考值，不是固定值**——个案值由 HR 在判定 Warning 等级的同一时刻设定，可按个案调整；Final Written 亦可个案改设具体到期日 | 个案值住主单侧「纪律记录有效期」字段——**未建**，依 Kent c50234 第 2 点划归 Alden／V1，不在本侧 13 个字段批次内。参考值目前只写在 Spec ⓪区与 A 表，**无工程落点** | 参考值改动＝改 Spec（Felix）；个案值＝HR 在 N07／N10／N14 填写 | 🔲 字段未建。该字段被 N13／N26／N25／C-6／C-7／D-6／D-7 七处消费，未建前这几处均无数据来源 |
| 8 | **到期前提醒＝5 天（C-6）** | 纪律处分记录到期前 5 天，系统发 D-7 知会（纯知会，不要求动作） | 🔲 N26 定时件未建，常量将住该件 → **数据化候选** | 改值＝改 Spec C-6（Felix） | 🔲 未建 |
| 9 | **打回次数上限＝2** | N07 最多打回补件 2 次；第 2 次后不得再打回，须直接作出最终结果 | 计数值住主单「打回次数」字段（**未建**，Alden／V1）；上限「2」是判定逻辑里的常量，将住 N07 审批卡回调件（**未建**，阻塞于模式九扩展）→ **数据化候选** | 改值＝改 Spec（N07 行与 C-2，Felix） | 🔲 未建。另见页首附表第 3 行（OSD-116 c50279 模式九扩展） |
| 10 | **Show Cause 延期次数上限＝1** | N10 最多批准 1 次合理延期；已批准 1 次后再逾期不再延期 | 计数值住 `customfield_18243`（Show Cause Extension Count，number，**已建并回读**）。Kent c50345 记该字段「init 0 / max 1」，但**本侧未验证该上限是否由字段层强制**——故按规则常量登记，落点为 N10 判定逻辑（未建）→ **数据化候选** | 改值＝改 Spec C-4（Felix） | 🔲 判定逻辑未建。字段层是否有上限约束，待实测或 Schema Owner 出具 |
| 11 | **Check-in 频率映射（C-10）＝15 天 PIP ≥1 次中期；30 天 ≥ 每 2 周 1 次；60／90 天 ≥ 每 30 天 1 次** | PIP 周期档位 → 最低 Check-in 频率 | 映射本体 🔲 未建，将住 N16 跟踪与提醒件 → **数据化候选**。个案的 Check-in 安排另存 `customfield_18251`（textfield 单行，已建并回读） | 改映射＝改 Spec C-10（Felix）。延长 PIP 后沿用原档对应频率（Kent c50343） | 🔲 未建 |
| 12 | **联合评审触发＝满 12 个月零违规；C-19 评审时限＝5 工作日** | Final Written 维持「永不自动失效」时，满 12 个月零违规由系统定时建 N25 任务卡，评审须在 5 工作日内完成 | 🔲 N25 定时建卡件未建，两个常量将住该件 → **数据化候选** | 改值＝改 Spec（⓪区八节与 C-19，Felix） | 🔲 未建 |
| 13 | **Final Outcome 九值** | Case 结案时写入的最终处置结果枚举（Spec ⓪区十五节九类） | 主单「Final Outcome」字段（**未建**，Kent c50234 第 2 点划归 Alden／V1）。枚举值目前只住 Spec | 字段建成后按 04.10 §五（Schema Owner）维护选项；枚举语义改动＝改 Spec（Felix） | 🔲 未建 |
| 14 | **计时口径（工作时／日历时）与 JSM Calendar 取值** | 哪些时限按工作时算、哪些按日历时算；工作时的「一天几小时、周几上班」取自哪份日历 | 口径逐条写在 Spec C 表「计时方式」列。工程侧住 JSM Calendar（主单侧）与 n8n 件（子单侧），**均未建** | 🔲 04.4 §8.3：Calendar 取值须与 NTP 工作时口径一致，偏差以 NTP 为准订正 Calendar；**NTP 工作时字段待 Alden**（页首附表） | 🔲 未建。与本表第 6 行同属页首附表第 21 行判据④ |
| 15 | **（非参数）**「同一次事件」「证据不足」「严重违纪标准」「合理延期原因」「提前终止条件」「PIP Failed 后降级或解雇」 | 这几项**没有数字门槛**，靠 HR 专业判断 | Spec ⑤ 引用区逐字：「属HR专业判断范畴，**不设机器可执行的数值门槛**……具体清单见《Nexmax WFH工作规章制度（正式版）》3.3节『处分执行原则』」。**该页本侧读不到**——页首附表第 16 行：Backend Operations 回 404、建造人账号回 403「Space is restricted」；正式存放位置与版本待流程 Owner 提供 | 不适用（非参数） | 不适用。**建设侧不得为这几项发明数值门槛，也不得建自动化拦截规则**；N05 查重同理——其判据为「同员工＋疑似同一次仍在处理中的事件」，Spec 未给任何时间窗口 |

---

## (3) Paragraf penutup — `insertNodeAfter`

**【本区与其他区的对应关系】**①第 6 行（SLA）与第 14 行（计时口径）对应**页首附表第 21 行**，该行四项解除判据即这两行的「生效方式」，不另立判据；②第 7、9、13 行的载体属**主单侧字段**，依 Kent c50234 第 2 点划归 Alden／V1，不在本侧 13 个执行卡／子单字段批次内（该批次见第三区）；③第 9 行另阻塞于模式九扩展——页首附表第 3 行，OSD-116 **c50279**，截至本次仍无 Alden 答复；④第 15 行所引人读权威页对应**页首附表第 16 行**。**将来若平台 Owner 为本流程建政策表**，动手前须读 07.06.1（现行 **v34**）**B2**「n8n Data Table 默认只回 50 行」：读全表必须显式带 limit 或分页，否则第 51 行起的政策**静默不生效**，既不报错也不提示被截断。
