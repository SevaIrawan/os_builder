# Draf Bagian 7 — 七、政策参数落位 (REV-2)

> Node target: `9302bb14-…` · 1 `replaceNode` + 2 `insertNodeAfter` · 8.487 karakter · 16 baris.
> REV-2 = setelah seluruh sumber dibaca habis (Spec v62 penuh, 04.10 v20, 04.4 v33, 04.5 v79, 04.9 v106, 07.06.1 v34).
> **Belum ditulis ke Confluence.**

## (1) Paragraf pembuka

🔲 待填。「**【2026-09-22 补·本区已填写】**本区按 04.5.2（现行 **v11**）五列登记：参数键／白话／住处／变更入口／生效方式；区职责同见 04.5（现行 **v79**）§2.1 第七区。上句「🔲 待填」为本次之前的状态，**原文保留不删**；自本次起以下表为准，未落位的格子在表内**逐格**标 🔲。依 04.5 §五「同一事实只有一个落点」，**本区不复述业务语义**——「白话」列只给一句可检索的标签，规则本体一律回指 Spec。三条前提先写在表外：**①落点不是「没有」，是「未登记消费」。**04.4（现行 **v33**）§十一 共享组件索引登记 `NOS ／ Platform ／ Policy Engine` 为**可用**，能力逐字「政策值读取唯一入口：按适用范围与生效日从 NOS Policy Store 取已解析的值（只读）」，契约页 Policy Engine 调用契约（现行 v2）。但 04.9（现行 **v106**）§四 现有唯一「政策表」`NOS Policy Store (F2 11-col)`（`DsK1slgPdy79LPHu`）用途逐字为「假期政策数值的运行时权威」，**无本流程的行**；本页第六区亦未登记 Policy Engine 为本流程调用件。故本流程要参数化，路径是**向平台 Owner 申请在 Policy Store 加行**（04.9 §四「建表权仅平台 Owner；建造线需要新表向平台 Owner 申请」）**并在第六区登记 Policy Engine**，不是自建私表。在此之前，工程侧数值按模板要求如实标「数据化候选」，与员工离职建造单第七区先例一致。**②「工作时」数值不进本区、也不进 Spec。**04.4 §8.3 定其为主数据、住 NTP、由 n8n 运行时读取；本区只登记该口径的落位与现状（见本表倒数第二行）。**③模板要求的「人读权威政策页」本流程暂缺。**Spec ⑤ 引用区指向《Nexmax WFH工作规章制度（正式版）》3.3节，该页本侧两个执行身份均读不到（页首附表第 16 行），正式位置与版本待流程 Owner 提供；在其补齐前，本区各行的业务含义只能回指 Spec。」

## (2) Tabel

| # | 参数键 | 白话 | 住处 | 变更入口 | 生效方式 |
|---|---|---|---|---|---|
| 1 | **PIP 延长周期＝15／30／60／90 天**（**限 N17 延长**） | 延长一次时可选的档位 | `customfield_18253` 选项集，住 **HR 专用 context 20118**（默认 Global 20112 已清空选项）。登记见 04.10 v20 §三；本侧 2026-09-22 以 `createmeta`（HR × `Sub-ticket` 14316）逐值回读 | 需求方开 BO Task 三格（04.10 §五 步骤 1）→ Schema Owner 审批、配置、回读并当场登记；业务口径改动须先经流程 Owner（本项 Felix c50328 已定「就 4 档」，Kent c50343 锁定） | 改该 context 的选项即时生效；消费方按字面值取，**不得在件内另写映射表** |
| 2 | **PIP 执行周期＝15／30／60／90 天（C-9，N07／N14 设定）** | 首次 PIP 的周期档位 | **🔲 无字段**。属 Spec A 表「PIP 参数」（改善问题／目标／衡量标准／**周期**／开始日期／到期日期／负责跟进人／Check-in 安排，收集节点 N07／N14）——**该组既不在本侧已建的 13 个执行卡／子单字段内，也不在 Kent c50283「不在本请求」所列的主单侧五项内**，归属未定。与上一行的 `cf18253` **是两个参数，勿混**（上行只管 N17 延长） | 🔲 归属未定：字段侧须先由 Schema Owner／V1 定归属；数值口径改动＝改 Spec C-9（Felix） | 🔲 无落点。值现由 N07 审批卡 modal 录入（c50279 ② 待 Alden 模式九扩展），写入目标字段须先存在并挂 edit 屏（Notify §9.14） |
| 3 | **Show Cause 解释结果＝成立／不成立** | N10 判解释成不成立 | `customfield_18252` 选项集，HR 专用 context **20117**（Global 20111 已清空） | 需求方开 BO Task 三格（04.10 §五 步骤 1）→ Schema Owner 审批、配置、回读并当场登记；业务口径改动须先经流程 Owner | 改该 context 的选项即时生效；消费方按字面值取，**不得在件内另写映射表** |
| 4 | **PIP Result＝Completed／Extension／Failed** | N17 的 PIP 过程结果 | `customfield_18254` 选项集，HR 专用 context **20119**（Global 20113 已清空） | 需求方开 BO Task 三格（04.10 §五 步骤 1）→ Schema Owner 审批、配置、回读并当场登记；业务口径改动须先经流程 Owner | 改该 context 的选项即时生效；消费方按字面值取，**不得在件内另写映射表** |
| 5 | **判定依据类型＝纪律违规／PIP未改善** | 解雇判定来自哪条路径 | `customfield_18255` 选项集，HR 专用 context **20120**（Global 20114 已清空） | 需求方开 BO Task 三格（04.10 §五 步骤 1）→ Schema Owner 审批、配置、回读并当场登记；业务口径改动须先经流程 Owner。**跨流程约束**：两值须与离职流「辞退原因」（Felix c50045）逐字一致 | 改该 context 的选项即时生效；消费方按字面值取，**不得在件内另写映射表**；**单改本侧即破坏与离职流的对齐**，改前须同时核对方字段 |
| 6 | **联合评审结果＝通过／不通过** | N25 联合评审结论 | `customfield_18257` 选项集，HR 专用 context **20121**（Global 20116 已清空）；挂 HR × `Task` 10004（屏 14877／14878） | 需求方开 BO Task 三格（04.10 §五 步骤 1）→ Schema Owner 审批、配置、回读并当场登记；业务口径改动须先经流程 Owner。字面同 Grade `cf18152` 但为独立字段、数据不共享（c50290 裁定不复用） | 改该 context 的选项即时生效；消费方按字面值取，**不得在件内另写映射表** |
| 7 | **SLA C-1～C-20（17 条）** | 各节点时限与超时升级对象 | 语义与数值的人读权威住 **Spec C 表**。工程载体依 04.4 v33 §8.1 按对象位置分工：SSCSD 主单走 **JSM 原生 SLA 引擎**；子单与跨对象时限走 **n8n 定时扫描件**。**两者均未建**（SSCSD-411 实测主单仍挂 Jira 默认 8h／16h）→ **数据化候选** | 改数值＝改 Spec C 表（Felix）；**建设侧不得改 Spec 语义**（04.5 v79 §五 写权分离）。工程侧照 Spec 配置由建造侧执行 | 🔲 未建。判据用页首附表第 21 行四项，不另立 |
| 8 | **纪律记录有效期 参考值＝Verbal 3 个月／Written 6 个月／Final Written 永不自动失效** | Warning 多久自动重置的**参考值**，个案由 HR 另设 | 个案值住主单侧「纪律记录有效期」字段（**未建**，c50234 第 2 点归 Alden／V1）。参考值现只在 Spec，**无工程落点** | 参考值＝改 Spec（Felix）；个案值＝HR 于 N07／N10／N14 填写 | 🔲 字段未建。被 N13／N25／N26／C-6／C-7／D-6／D-7 七处消费 |
| 9 | **到期前提醒＝5 天（C-6）** | 到期前几天发 D-7 | 🔲 N26 定时件未建 → **数据化候选** | 改值＝改 Spec C-6（Felix） | 🔲 未建 |
| 10 | **打回次数上限＝2** | N07 最多打回几次 | 计数值住主单「打回次数」字段（**未建**，Alden／V1）；上限「2」是 N07 判定逻辑里的常量（件未建，阻塞于模式九扩展）→ **数据化候选**。业务签第 12 轮（Kayden，2026-09-04）明定该规则**须被机械执行**，不得只留文字 | 改值＝改 Spec（N07 行与 C-2，Felix） | 🔲 未建。另见页首附表第 3 行（c50279） |
| 11 | **Show Cause 延期次数上限＝1** | N10 最多批准几次延期 | 计数值住 `customfield_18243`（number，**已建并回读**）。上限「1」已随字段登记于 04.10 v20 §三（逐字「number·初始0·最多1·系统写」），但**该登记是规则登记，Jira number 字段本身无最大值约束**——强制点在 N10 判定逻辑（**未建**）→ **数据化候选** | 改值＝改 Spec C-4（Felix） | 🔲 判定逻辑未建。字段层不设约束，重复批准不会被 Jira 拦 |
| 12 | **Check-in 频率映射（C-10）＝15 天≥1 次中期；30 天≥每 2 周 1 次；60／90 天≥每 30 天 1 次** | 周期档位 → 最低 Check-in 频率 | 映射 🔲 未建，将住 N16 跟踪提醒件 → **数据化候选**。个案安排另存 `customfield_18251`（单行文本，已建） | 改映射＝改 Spec C-10（Felix）。延长后沿用原档频率（c50343） | 🔲 未建 |
| 13 | **联合评审触发＝满 12 个月零违规；C-19＝5 工作日** | 何时建 N25 卡、评审多久 | 🔲 N25 定时建卡件未建 → **数据化候选**。另：「零违规」的检索范围 Spec 需技术确认项第 5 条列为待与 Alden 核实 | 改值＝改 Spec（⓪区八节与 C-19，Felix） | 🔲 未建 |
| 14 | **Final Outcome 九值** | 结案时的最终处置结果枚举 | 主单「Final Outcome」字段（**未建**，c50234 第 2 点归 Alden／V1）；枚举现只住 Spec ⓪区十五节 | 字段建成后按 04.10 §五；枚举语义＝改 Spec（Felix） | 🔲 未建 |
| 15 | **工作时口径与 JSM Calendar 取值** | 工作时怎么算、取自哪份日历 | **数值住 NTP（Registry），不住文档**——04.4 v33 §8.3 逐字：「其具体口径（每日上下班时间、午休扣除、周末与公众假期）是人员与组织的主数据，住 NTP，由 n8n 在运行时读取，不在标准页、政策页或 Spec 里登记数值」。Spec C 表只写「工作时／日历时」类型。JSM Calendar 的取值**须与 NTP 一致，偏差以 NTP 为准订正 Calendar** | NTP 字段定义（建设侧查表，不逐次问流程 Owner，§8.3）；**NTP 工作时字段待 Alden**（页首附表） | 🔲 未建。**另有平台级未决**：04.4 §8.3 挂 🔲 待裁——「请假政策页写『3 个工作日』，现网实际按『3 天日历天』计时，两处口径不一致，待流程 Owner 与 Alden 侧对齐」；本流程 17 条 SLA 多数标「工作时」，该待裁一日不决，本行口径即未落定 |
| 16 | **（非参数）**「同一次事件」「证据不足」「严重违纪标准」「合理延期原因」「提前终止条件」「PIP Failed 后降级或解雇」 | 这几项没有数字门槛 | Spec ⑤ 引用区逐字：「属HR专业判断范畴，**不设机器可执行的数值门槛**……具体清单见《Nexmax WFH工作规章制度（正式版）》3.3节『处分执行原则』」。**该页本侧读不到**（页首附表第 16 行：404／403「Space is restricted」），正式位置与版本待流程 Owner 提供 | 不适用（非参数） | 不适用。**建设侧不得为这几项发明数值门槛或建自动拦截规则**；N05 查重同理，其判据为「同员工＋疑似同一次仍在处理中的事件」，Spec 未给时间窗口 |

## (3) Paragraf penutup

**【本区与其他区的对应关系】**①第 7 行（SLA）与第 15 行（工作时口径）对应**页首附表第 21 行**，该行四项解除判据即这两行的「生效方式」，不另立；②第 2、8、10、14 行的载体属**尚未建成的字段**：第 8、10、14 行依 Kent c50234 第 2 点归 Alden／V1（主单侧），**第 2 行「PIP 参数」组归属未定**——既不在本侧已建的 13 个执行卡／子单字段内，也不在 c50283「不在本请求」所列的主单侧五项内，须另行厘清；③第 10 行另阻塞于模式九扩展（页首附表第 3 行，OSD-116 **c50279**）；④第 16 行所引人读权威页对应**页首附表第 16 行**。**将来若平台 Owner 为本流程在 Policy Store 加行**，动手前须读 07.06.1（现行 **v34**）**B2**「n8n Data Table 默认只回 50 行」：读全表必须显式带 limit 或分页，否则第 51 行起的政策**静默不生效**，既不报错也不提示被截断。
